name: Показать время

on:
  push:
    branches: "*"
  pull_request:
    branches: "*"

jobs:
  show-current-time:
    runs-on: ubuntu-latest
    steps:
      - name: Вывести дату и время
        run: date
